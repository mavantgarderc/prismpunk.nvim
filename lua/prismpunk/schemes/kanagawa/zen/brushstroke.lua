local color = require("prismpunk.utils.color")

local palette = {
  groundBlack = "#080806",
  groundDeep = "#121210",
  groundInk = "#1E1E1C",
  groundChar = "#2E2E2C",
  groundAsh = "#424240",
  strokeDark = "#5E5E5C",
  strokeMid = "#848480",
  strokeLight = "#B0ACA8",
  strokePale = "#D8D4D0",
  strokeWhite = "#F0EDE8",
  indigoDeep = "#4068B0",
  indigoLight = "#6090D0",
  vermDeep = "#C03830",
  vermBright = "#E05848",
  ochreDeep = "#D8A830",
  ochreBright = "#F8D060",
  bg_darkest = "#080806",
  fg_lightest = "#F0EDE8",
}

local M = {}

---@param opts table|nil
---@param plt table|nil
---@return table
M.get = function(opts, plt)
  plt = plt or palette
  opts = opts or {}
  return {
    modes = {
      normal = plt.strokeLight,
      insert = plt.strokePale,
      visual = plt.indigoDeep,
      replace = plt.vermBright,
      command = plt.strokeWhite,
    },
    accent = {
      accent1 = plt.indigoDeep,
      accent2 = plt.indigoLight,
      accent3 = plt.ochreDeep,
      accent4 = plt.strokePale,
      accent5 = plt.strokeWhite,
      invert = plt.groundBlack,
    },
    rainbow = {
      rainbow1 = plt.vermBright,
      rainbow2 = plt.vermDeep,
      rainbow3 = plt.ochreBright,
      rainbow4 = plt.ochreDeep,
      rainbow5 = plt.indigoLight,
      rainbow6 = plt.indigoDeep,
      rainbow7 = plt.strokeMid,
    },
    ui = {
      fg = plt.strokePale,
      fg_dim = plt.strokeLight,
      fg_dimmer = plt.strokeMid,
      fg_dark = plt.strokeDark,
      fg_reverse = plt.groundBlack,
      bg = plt.groundBlack,
      bg_dim = plt.groundBlack,
      bg_m1 = plt.groundDeep,
      bg_m2 = plt.groundInk,
      bg_m3 = plt.groundChar,
      bg_m4 = plt.groundAsh,
      bg_p1 = plt.groundInk,
      bg_p2 = plt.groundChar,
      bg_gutter = (opts.gutter ~= false) and plt.groundDeep or "none",
      bg_cursorline = plt.groundInk,
      bg_cursorline_alt = plt.groundChar,
      cursorline = plt.groundInk,
      bg_highlight = plt.groundChar,
      bg_search = plt.indigoDeep,
      bg_visual = plt.groundChar,
      bg_statusline = plt.groundDeep,
      border = plt.groundChar,
      header1 = plt.strokeWhite,
      header2 = plt.indigoLight,
      special = plt.indigoDeep,
      nontext = plt.groundChar,
      whitespace = plt.groundAsh,
      win_separator = plt.strokeDark,
      indent = plt.groundInk,
      indent_scope = plt.indigoDeep,
      picker = plt.indigoLight,
      yank = plt.ochreBright,
      mark = plt.indigoDeep,
      scrollbar = plt.groundChar,
      selection = plt.groundChar,
      line_nr = plt.strokeMid,
      line_nr_dim = plt.strokeDark,
      line_nr_active = plt.strokeWhite,
      float = {
        fg = plt.strokeLight,
        bg = plt.groundChar,
        fg_border = plt.strokeDark,
        bg_border = plt.groundChar,
      },
      pmenu = {
        fg = plt.strokePale,
        fg_sel = plt.strokeWhite,
        fg_border = plt.strokeDark,
        bg = plt.groundInk,
        bg_sel = plt.groundChar,
        bg_border = plt.groundInk,
        bg_sbar = plt.groundInk,
        bg_thumb = plt.strokeDark,
      },
      tabline = {
        bg = plt.groundBlack,
        bg_inactive = plt.groundBlack,
        bg_selected = plt.groundInk,
        bg_alternate = plt.groundDeep,
        fg_inactive = plt.strokeDark,
        fg_selected = plt.strokeWhite,
        fg_alternate = plt.indigoLight,
        indicator = plt.vermBright,
      },
    },
    syn = {
      attribute = plt.strokeLight,
      boolean = plt.vermBright,
      comment = plt.strokeDark,
      constant = plt.strokePale,
      deprecated = plt.strokeDark,
      func = plt.strokeWhite,
      identifier = plt.strokeLight,
      keyword = plt.indigoLight,
      method = plt.strokeWhite,
      number = plt.ochreBright,
      operator = plt.strokeMid,
      parameter = plt.strokeLight,
      preproc = plt.indigoDeep,
      punct = plt.strokeMid,
      regex = plt.ochreBright,
      special = plt.indigoDeep,
      special2 = plt.vermDeep,
      special3 = plt.ochreDeep,
      statement = plt.indigoLight,
      string = plt.strokeLight,
      symbol = plt.ochreDeep,
      type = plt.strokePale,
      variable = plt.strokePale,
    },
    diag = {
      error = plt.vermBright,
      warning = plt.ochreBright,
      info = plt.indigoLight,
      hint = plt.strokeMid,
      ok = plt.strokePale,
      virtual_text_error = plt.vermDeep,
      virtual_text_warning = plt.ochreDeep,
      virtual_text_info = plt.indigoDeep,
      virtual_text_hint = plt.strokeDark,
      virtual_text_ok = plt.strokeLight,
    },
    diff = {
      add = plt.strokePale,
      add_inline = plt.strokeWhite,
      change = plt.indigoLight,
      change_inline = plt.indigoDeep,
      delete = plt.vermBright,
      delete_inline = plt.vermDeep,
      text = plt.strokeLight,
    },
    vcs = {
      added = plt.strokePale,
      changed = plt.indigoLight,
      removed = plt.vermBright,
    },
    term = {
      black = plt.groundBlack,
      red = plt.vermBright,
      green = plt.strokePale,
      yellow = plt.ochreBright,
      blue = plt.indigoLight,
      magenta = plt.indigoDeep,
      cyan = plt.strokeLight,
      white = plt.strokeWhite,
      black_bright = color(plt.groundBlack):brighten(0.6):to_hex(),
      red_bright = color(plt.vermBright):brighten(0.2):to_hex(),
      green_bright = color(plt.strokePale):brighten(0.1):to_hex(),
      yellow_bright = color(plt.ochreBright):brighten(0.1):to_hex(),
      blue_bright = color(plt.indigoLight):brighten(0.2):to_hex(),
      magenta_bright = color(plt.indigoDeep):brighten(0.2):to_hex(),
      cyan_bright = color(plt.strokeLight):brighten(0.1):to_hex(),
      white_bright = color(plt.strokeWhite):brighten(0.1):to_hex(),
      indexed1 = plt.vermDeep,
      indexed2 = plt.ochreDeep,
    },
  }
end

return {
  name = "Kanagawa Zen – Brushstroke",
  author = "PrismPunk.nvim",
  description = "Monochromatic dark with expressive mid-grey accents — the brush loaded and dragged, indigo and vermillion against warm ash.",

  base16 = {
    base00 = palette.groundBlack,
    base01 = palette.groundDeep,
    base02 = palette.groundInk,
    base03 = palette.groundChar,
    base04 = palette.strokeMid,
    base05 = palette.strokePale,
    base06 = palette.strokeLight,
    base07 = palette.strokeWhite,
    base08 = palette.vermBright,
    base09 = palette.ochreBright,
    base0A = palette.indigoLight,
    base0B = palette.strokePale,
    base0C = palette.strokeLight,
    base0D = palette.indigoDeep,
    base0E = palette.indigoLight,
    base0F = palette.ochreDeep,
  },

  palette = palette,
  get = M.get,
}
