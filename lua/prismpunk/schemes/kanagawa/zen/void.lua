local color = require("prismpunk.utils.color")

local palette = {
  voidBlack = "#040404",
  voidDeep = "#0C0C0C",
  voidInk = "#181818",
  voidChar = "#282828",
  voidAsh = "#383838",
  greyDark = "#484848",
  greyMid = "#606060",
  mossDark = "#2A4A3A",
  mossDeep = "#385848",
  mossMid = "#487860",
  mossRoot = "#183828",
  mossGround = "#386050",
  mossLight = "#80A898",
  mossPale = "#B0C8C0",
  nightDeep = "#101020",
  nightChar = "#202038",
  bg_darkest = "#040404",
  fg_lightest = "#B0C8C0",
}

local M = {}

---@param opts table|nil
---@param plt table|nil
---@return table
M.get = function(opts, plt)
  plt = plt or palette
  opts = opts or {}
  return {
    modes = {
      normal = plt.mossLight,
      insert = plt.mossPale,
      visual = plt.mossMid,
      replace = plt.mossLight,
      command = plt.mossPale,
    },
    accent = {
      accent1 = plt.mossMid,
      accent2 = plt.mossLight,
      accent3 = plt.mossPale,
      accent4 = plt.greyMid,
      accent5 = plt.mossPale,
      invert = plt.voidBlack,
    },
    rainbow = {
      rainbow1 = plt.mossPale,
      rainbow2 = plt.mossLight,
      rainbow3 = plt.mossMid,
      rainbow4 = plt.mossGround,
      rainbow5 = plt.mossDeep,
      rainbow6 = plt.greyMid,
      rainbow7 = plt.greyDark,
    },
    ui = {
      fg = plt.mossLight,
      fg_dim = plt.mossMid,
      fg_dimmer = plt.mossDeep,
      fg_dark = plt.mossDark,
      fg_reverse = plt.voidBlack,
      bg = plt.voidBlack,
      bg_dim = plt.voidBlack,
      bg_m1 = plt.voidDeep,
      bg_m2 = plt.voidInk,
      bg_m3 = plt.voidChar,
      bg_m4 = plt.voidAsh,
      bg_p1 = plt.voidInk,
      bg_p2 = plt.voidChar,
      bg_gutter = (opts.gutter ~= false) and plt.voidDeep or "none",
      bg_cursorline = plt.voidInk,
      bg_cursorline_alt = plt.voidChar,
      cursorline = plt.voidInk,
      bg_highlight = plt.voidChar,
      bg_search = plt.mossRoot,
      bg_visual = plt.voidChar,
      bg_statusline = plt.voidDeep,
      border = plt.voidChar,
      header1 = plt.mossPale,
      header2 = plt.mossLight,
      special = plt.mossMid,
      nontext = plt.voidChar,
      whitespace = plt.voidAsh,
      win_separator = plt.greyDark,
      indent = plt.voidInk,
      indent_scope = plt.mossDark,
      picker = plt.mossMid,
      yank = plt.mossPale,
      mark = plt.mossMid,
      scrollbar = plt.voidChar,
      selection = plt.voidChar,
      line_nr = plt.greyDark,
      line_nr_dim = plt.voidAsh,
      line_nr_active = plt.mossLight,
      float = {
        fg = plt.mossLight,
        bg = plt.voidChar,
        fg_border = plt.greyDark,
        bg_border = plt.voidChar,
      },
      pmenu = {
        fg = plt.mossLight,
        fg_sel = plt.mossPale,
        fg_border = plt.greyDark,
        bg = plt.voidInk,
        bg_sel = plt.voidChar,
        bg_border = plt.voidInk,
        bg_sbar = plt.voidInk,
        bg_thumb = plt.greyDark,
      },
      tabline = {
        bg = plt.voidBlack,
        bg_inactive = plt.voidBlack,
        bg_selected = plt.voidInk,
        bg_alternate = plt.voidDeep,
        fg_inactive = plt.greyDark,
        fg_selected = plt.mossPale,
        fg_alternate = plt.mossLight,
        indicator = plt.mossMid,
      },
    },
    syn = {
      attribute = plt.mossLight,
      boolean = plt.mossLight,
      comment = plt.greyDark,
      constant = plt.mossPale,
      deprecated = plt.greyDark,
      func = plt.mossPale,
      identifier = plt.mossLight,
      keyword = plt.mossLight,
      method = plt.mossPale,
      number = plt.mossLight,
      operator = plt.greyMid,
      parameter = plt.mossMid,
      preproc = plt.mossMid,
      punct = plt.greyMid,
      regex = plt.mossLight,
      special = plt.mossMid,
      special2 = plt.mossDeep,
      special3 = plt.mossDark,
      statement = plt.mossLight,
      string = plt.mossMid,
      symbol = plt.mossMid,
      type = plt.mossPale,
      variable = plt.mossLight,
    },
    diag = {
      error = plt.mossLight,
      warning = plt.mossPale,
      info = plt.mossMid,
      hint = plt.greyMid,
      ok = plt.mossLight,
      virtual_text_error = plt.mossDark,
      virtual_text_warning = plt.mossDeep,
      virtual_text_info = plt.mossDark,
      virtual_text_hint = plt.greyDark,
      virtual_text_ok = plt.mossDark,
    },
    diff = {
      add = plt.mossMid,
      add_inline = plt.mossLight,
      change = plt.mossDeep,
      change_inline = plt.mossMid,
      delete = plt.greyMid,
      delete_inline = plt.greyDark,
      text = plt.mossLight,
    },
    vcs = {
      added = plt.mossMid,
      changed = plt.mossDeep,
      removed = plt.greyMid,
    },
    term = {
      black = plt.voidBlack,
      red = plt.mossLight,
      green = plt.mossPale,
      yellow = plt.mossLight,
      blue = plt.mossMid,
      magenta = plt.mossDeep,
      cyan = plt.mossLight,
      white = plt.mossPale,
      black_bright = color(plt.voidBlack):brighten(0.6):to_hex(),
      red_bright = color(plt.mossLight):brighten(0.1):to_hex(),
      green_bright = color(plt.mossPale):brighten(0.1):to_hex(),
      yellow_bright = color(plt.mossLight):brighten(0.1):to_hex(),
      blue_bright = color(plt.mossMid):brighten(0.2):to_hex(),
      magenta_bright = color(plt.mossDeep):brighten(0.3):to_hex(),
      cyan_bright = color(plt.mossLight):brighten(0.1):to_hex(),
      white_bright = color(plt.mossPale):brighten(0.1):to_hex(),
      indexed1 = plt.nightDeep,
      indexed2 = plt.nightChar,
    },
  }
end

return {
  name = "Kanagawa Zen – Void",
  author = "PrismPunk.nvim",
  description = "Deep black, single grey, one muted accent — Zen emptiness. Moss barely visible in absolute dark, the colour of absence.",

  base16 = {
    base00 = palette.voidBlack,
    base01 = palette.voidDeep,
    base02 = palette.voidInk,
    base03 = palette.voidChar,
    base04 = palette.greyDark,
    base05 = palette.mossLight,
    base06 = palette.mossPale,
    base07 = palette.mossPale,
    base08 = palette.mossLight,
    base09 = palette.mossMid,
    base0A = palette.mossPale,
    base0B = palette.mossLight,
    base0C = palette.mossMid,
    base0D = palette.mossLight,
    base0E = palette.mossMid,
    base0F = palette.nightDeep,
  },

  palette = palette,
  get = M.get,
}
